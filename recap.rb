# SCHEMA DESIGN

# A company sells products to customers
# Products have name, description and price
# Customers have email and phone
# Customers can have multiple orders
# An order is only of one customer
# An order can have multiple products
# A product can be in multiple orders


# SQL

# SELECT column(s) -> SELECT first_name, last_name
# FROM table(s) -> FROM patients
  # JOIN table(s) ->  JOIN consultations ON c.patient_id = p.id (optional)
# WHERE condition(s) -> WHERE age = 21 (optional)
# GROUP BY column(s) -> GROUP BY age (optional)
# ORDER BY column(s) -> ORDER BY age (optional)

# Questions about the exercises?
